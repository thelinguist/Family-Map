create Async tasks for downloading stuff. It creates a background thread
login
people
events
ex.
        AsyncTask<String...args,Integer,Return Value>
        doinBackground(String...args) {

        }

        person activity
        map activity is different than fragment, cuz it has a menu

        in a fragment you can do .getActivity()

        to make an intent:
Intents i = new Intent(context, ClassName.class) //class name is where we are going ex. personActivity.class
i.putExtra("id",personID); // this puts stuff to bundle, just strings
StartActivity(i);

getIntent.getStringExtra("id"); //getIntent or getBundle, the TA doesn't know

getArguments().getStringExtra("id");





//expand or hide list
//have:
private RecyclerView family;
textView.setOnClicListener(View v blah) {
    onClick(View v) {
        family.setVisibility(family.isShown()? View.Gone:View.visible);     //? x:y is an inline if statement, if true, do x, else do y
        if (family.isShown()) {
            familyDirectionArrow.setImageResource(R.image.drawable.up);
        }
        else point it up.
    }
}


menu
    item

//in activity, set visibility stuff



create an intent to main activity
set flag: Intent.FLAG_ACTIVITY_CLEAR_TOP               //this flag will know to clear all activities.
//dont forget



test for more events
    <string name="default_server">192.168.252.67</string>
    <string name="default_port">8080</string>
    <string name="default_username">brigham1</string>
    <string name="default_password">password</string>


    <string name="default_server">192.168.14.40</string>
    <string name="default_port">8024</string>
    <string name="default_username">sheila</string>
    <string name="default_password">parker</string>

